{
  "name": "mayfly",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "bin": {
    "mf": "src/cli/index.ts"
  },
  "scripts": {
    "mf": "bun run src/cli/index.ts",
    "build": "bun build --compile src/cli/index.ts --outfile dist/mf",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
